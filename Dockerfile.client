FROM node:20-alpine

RUN npm install -g serve

WORKDIR /app

COPY . .

RUN npm install --include=dev

RUN npx turbo run build --filter=tma-client

EXPOSE 53002

CMD ["serve", "-s", "apps/tma-client/dist", "-p", "53002"]
